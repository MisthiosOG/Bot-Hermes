{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn run:application --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --threads 2",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}